function CTA() {
	return (
		<section id="contact" className="CTA">
			{/* CTA / Contact content — replace with your contact form or CTA */}
			<h2>Contact Us</h2>
		</section>
	);
}

export default CTA;
